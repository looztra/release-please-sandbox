["precommit:install"]
description = "Install the pre-commit hooks"
run = "pre-commit install"

["precommit:run"]
description = "Run the pre-commit hooks on all files"
run = "pre-commit run --show-diff-on-failure --color=always --all-files"
